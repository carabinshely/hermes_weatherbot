name: Apply PR 53 forecast-vintage fix

on:
  push:
    branches:
      - agent/issue-48-calibrated-runtime-boundary
    paths:
      - .github/workflows/pr53-codex-vintage.yml

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v6
        with:
          ref: agent/issue-48-calibrated-runtime-boundary

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install locked development environment
        run: uv sync --locked --all-groups

      - name: Apply forecast-vintage fix
        run: python scripts/pr53_codex_vintage_patch.py

      - name: Format and lint
        run: |
          uv run ruff check --fix weatherbot/forecasting/contracts.py weatherbot/forecasting/archive.py weatherbot/forecasting/runtime.py bot_v3.py tests/forecasting/test_calibration_runtime.py tests/forecasting/test_bot_source.py tests/forecasting/test_archive.py
          uv run ruff format weatherbot/forecasting/contracts.py weatherbot/forecasting/archive.py weatherbot/forecasting/runtime.py bot_v3.py tests/forecasting/test_calibration_runtime.py tests/forecasting/test_bot_source.py tests/forecasting/test_archive.py
          uv run ruff check execution_modes.py runtime_security.py weatherbot tests
          uv run ruff format --check execution_modes.py runtime_security.py weatherbot tests

      - name: Type-check
        run: uv run pyright execution_modes.py runtime_security.py weatherbot tests

      - name: Focused regressions
        run: PYTHONPATH=. uv run pytest -q tests/forecasting/test_calibration_runtime.py tests/forecasting/test_bot_source.py tests/forecasting/test_archive.py tests/forecasting/test_calibrated_scanner_gate.py tests/test_minimal_import_boundary.py

      - name: Full suite
        run: PYTHONPATH=. uv run pytest -q

      - name: Commit verified fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add weatherbot/forecasting/contracts.py weatherbot/forecasting/archive.py weatherbot/forecasting/runtime.py bot_v3.py tests/forecasting/test_calibration_runtime.py tests/forecasting/test_bot_source.py tests/forecasting/test_archive.py docs/forecast-calibration.md
          git commit -m "Align runtime probabilities with calibrated forecast vintage"
          git push origin HEAD:agent/issue-48-calibrated-runtime-boundary
