name: Fix persistence Pyright

on:
  push:
    branches: [agent/durable-event-store]

permissions:
  contents: write

jobs:
  fix:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v6
        with:
          ref: agent/durable-event-store

      - name: Apply strict typing patch
        run: python tools/fix_persistence_pyright.py

      - name: Format changed files
        run: |
          python -m pip install --quiet ruff==0.15.22
          ruff check --fix weatherbot/persistence tests/persistence
          ruff format weatherbot/persistence tests/persistence

      - name: Commit typing fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A -- ':!tools/fix_persistence_pyright.py' ':!.github/workflows/fix-persistence-pyright.yml'
          git commit -m "fix: satisfy strict persistence typing"
          git push origin HEAD:agent/durable-event-store
